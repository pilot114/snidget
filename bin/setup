#!/usr/bin/php
<?php

/**
 * Этот скрипт предназначен для первоначальной установки фреймворка:
 * - установка и первоначальная конфигурация инфраструктуры (Dockerfile + docker-compose.yml)
 * - выбор и подключение модулей (Boxes)
 */
